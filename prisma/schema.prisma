// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TEAM_TASKS_TODO_1:
// Define User and Task models for the Team Tasks Dashboard.
// Requirements:
// - User: id (string, cuid), name (string), email (string, unique), createdAt
// - Task: id (string, cuid), title (string), description (string?), completed (boolean), createdAt, updatedAt
// - Relation: Task belongs to a User via assignedToId (many tasks per user).
// - Consider appropriate indexes for querying by user and completion.
